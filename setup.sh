#!/bin/bash

ln -si `dirname $0`/.vimrc $HOME
ln -si `dirname $0`/.pryrc $HOME
ln -si `dirname $0`/.tmux.conf $HOME
ln -si `dirname $0`/.psqlrc $HOME
ln -si `dirname $0`/.zshrc $HOME
ln -si `dirname $0`/.bashrc $HOME

